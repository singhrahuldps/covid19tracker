on:
    schedule:
      # * is a special character in YAML so you have to quote this string
      - cron:  '* * * * *'

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
    - name: Run the script 
      id: runscript
      uses: actions/covid19trackeraction